<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EventHub</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
<%- include('../partials/_navbar') %>

  <h1>Edit Event</h1>
  <form action="/events/<%= event._id %>?_method=PUT" method="POST">
    <label>Title: <input name="title" value="<%= event.title %>" required></label><br>
    <label>Date & Time: <input type="datetime-local" name="date" value="<%= event.date ? new Date(event.date).toISOString().slice(0,16) : '' %>" required></label><br>
    <label>Location: <input name="location" value="<%= event.location %>" required></label><br>
    <label>Description:<br><textarea name="description"><%= event.description %></textarea></label><br>
    <button type="submit">Save</button>
  </form>

<%- include('../partials/_footer') %>
</body>
</html>
